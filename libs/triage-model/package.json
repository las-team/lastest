{
  "name": "@lastest/triage-model",
  "version": "0.1.0",
  "private": true,
  "description": "The pure triage model: verdict vocabulary, group kinds, group evidence and the deterministic pre-pass clustering the LLM refines. No DB, no clock, no AI client — the third tier of docs/architecture/core-scope.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.0"
  },
  "license": "FSL-1.1-ALv2"
}
