{
  "name": "@lastest/test-templates",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical Playwright test bodies emitted by the agents (auth-setup, walkthrough) plus the CTA-safety patterns. Pure string rendering, dependency-free — the third tier of docs/architecture/core-scope.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.0"
  },
  "license": "FSL-1.1-ALv2"
}
