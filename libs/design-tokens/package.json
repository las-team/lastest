{
  "name": "@lastest/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Design-token parsing, value normalization and config merging: a CSS custom-property block → a DesignSystemConfig the EB harvester matches computed styles against. Pure string/heuristic work over eb-protocol types — no DB, no browser, no credential resolution — the third tier of docs/architecture/core-scope.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lastest/eb-protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "license": "FSL-1.1-ALv2"
}
