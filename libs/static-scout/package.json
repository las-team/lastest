{
  "name": "@lastest/static-scout",
  "version": "0.1.0",
  "private": true,
  "description": "Static, no-browser HTML scout: fetch a page's HTML and extract a best-effort selector map (headings, forms, links, testids) with no JS rendering. No DB, no browser, no credential resolution — the third tier of docs/architecture/core-scope.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "license": "FSL-1.1-ALv2"
}
