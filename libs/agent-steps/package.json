{
  "name": "@lastest/agent-steps",
  "version": "0.1.0",
  "private": true,
  "description": "Step-shape helpers for agent sessions (active step, one-line narration, progress). Pure, dependency-free, gate-free — the third tier of docs/architecture/core-scope.md §3. One copy so the Agents console and the plugin UIs never disagree about which step is current or what sentence describes it.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.0"
  },
  "license": "FSL-1.1-ALv2"
}
