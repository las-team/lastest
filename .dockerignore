# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
dist
!packages/embedded-browser/dist
*.tsbuildinfo

# Database (handled via volumes)
*.db
*.db-shm
*.db-wal

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp
*.swo

# Test outputs
test-output
coverage

# Development
.env.local
.env.development
.claude
.auto-claude
.auto-claude-status
.claude_settings.json
.playwright-mcp
.worktrees

# Documentation (not needed in image)
docs
*.md
!README.md

# Scripts (not needed in build except entrypoint)
scripts/*.ts

# CI / infra manifests — not part of the app build context
k8s
.github

# Sibling services / packages the app image doesn't build (the builder stage
# rm -rf's these anyway; keeping them out shrinks the COPY . . context tarball)
packages/ocr-service
packages/vscode-extension
packages/runner

# Large local artifacts
storage
.turbo

# Misc
*.log
*.tmp
Thumbs.db
.DS_Store
