name: Visual Regression Tests

on:
  pull_request:
    branches: [main]

jobs:
  # Remote mode: Dispatch to existing runner (recommended)
  # Uncomment this job and configure secrets/variables to use remote runner
  # remote-regression:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Run visual regression tests
  #       id: visual
  #       uses: ./action
  #       with:
  #         server-url: ${{ secrets.LASTEST_SERVER_URL }}
  #         runner-token: ${{ secrets.LASTEST_RUNNER_TOKEN }}
  #         repo-id: ${{ vars.LASTEST_REPO_ID }}
  #         team-id: ${{ vars.LASTEST_TEAM_ID }}
  #         runner-id: ${{ vars.LASTEST_RUNNER_ID }}
  #         timeout: '300'
  #         fail-on-changes: 'false'
  #
  #     - name: Summary
  #       if: always()
  #       run: |
  #         echo "## Visual Regression Results" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "**Status:** ${{ steps.visual.outputs.status }}" >> $GITHUB_STEP_SUMMARY
  #         echo "**Passed:** ${{ steps.visual.outputs.passed-count }}" >> $GITHUB_STEP_SUMMARY
  #         echo "**Failed:** ${{ steps.visual.outputs.failed-count }}" >> $GITHUB_STEP_SUMMARY
  #         echo "**Changes:** ${{ steps.visual.outputs.changed-count }}" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "[View Results](${{ steps.visual.outputs.build-url }})" >> $GITHUB_STEP_SUMMARY

  # Local mode: Run Playwright directly in CI (requires browser installation)
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium

      - name: Setup database
        run: pnpm db:push

      - name: Build application
        run: pnpm build

      # Visual regression test step removed — re-enable once vars.REPO_ID is configured
      # - name: Run visual regression tests
      #   run: |
      #     node .next/standalone/server.js &
      #     sleep 10
      #     npx tsx scripts/run-tests.ts --repo-id "${{ vars.REPO_ID }}" --base-url http://localhost:3000 --output-dir ./test-output
      #   env:
      #     DATABASE_PATH: ./lastest2.db
      #     PORT: 3000
      #     HOSTNAME: 0.0.0.0
      #
      # - name: Upload screenshots
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: regression-screenshots
      #     path: ./test-output/
      #     retention-days: 7
