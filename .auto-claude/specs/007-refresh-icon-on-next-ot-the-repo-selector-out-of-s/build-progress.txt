# Build Progress: Move Refresh Icon Next to Repo Selector

## Subtask 1-1: Locate and identify refresh icon in sidebar and repo selector components

**Status:** COMPLETED

### Findings

#### Refresh Icon Location
- **File:** `src/components/layout/repo-selector.tsx`
- **Lines:** 87-95
- **Component:** Button with RefreshCw icon from lucide-react
- **Details:**
  - The refresh icon is INSIDE the RepoSelector component (not directly in the sidebar)
  - Uses `<Button variant="ghost" size="icon">` with `<RefreshCw>` icon
  - Triggers `handleSync()` function which calls `fetchAndSyncRepos()` server action
  - Has spinning animation when syncing: `${isSyncing ? 'animate-spin' : ''}`
  - Title: "Sync repositories from GitHub"

#### RepoSelector Component Structure (repo-selector.tsx)
```jsx
<div className="flex items-center gap-2">
  <Select>...</Select>           // Repository dropdown
  <Button>                        // <-- REFRESH ICON IS HERE
    <RefreshCw />
  </Button>
</div>
```

#### Sidebar Component (sidebar.tsx)
- **File:** `src/components/layout/sidebar.tsx`
- **Line 39:** Contains `<RepoSelector />` inside `<div className="p-4 border-b">`
- The sidebar doesn't directly have the refresh icon - it's nested inside RepoSelector

#### Layout Structure (layout.tsx)
- Main layout: `<div className="flex h-screen">`
- Sidebar: `<Sidebar />` (w-64, left side)
- Main content: `<main className="flex-1 overflow-auto">{children}</main>` (right side)

### Files to Modify for Subtask 1-2
1. **src/components/layout/repo-selector.tsx** - Remove refresh button from RepoSelector
2. **Sidebar or Header area** - Add refresh button next to repo selector in main content area

### Notes
- The refresh functionality (handleSync, isSyncing state) will need to be extracted/shared
- May need to expose sync state via props or context
- Consider adding refresh button to the Header component or creating a new component in main content area
